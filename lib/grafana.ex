defmodule Grafana do
  use Grafana.API

  @docmodule """
  Tools for getting data from the Grafana API as well as chart objects for
  embedding.

  Each Grafana API query accesses a different URL, not just different query
  arguments, so for clarity each type of Grafana API call gets its own
  function.
  """


end
